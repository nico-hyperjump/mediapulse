Generate presenntation in watch mode:

```
pnpm dlx @marp-team/marp-cli@latest -w docs/presentation-marp.md
```

Then open the docs/presentation-marp.html file in your browser.

Generate pptx file:

```
pnpm dlx @marp-team/marp-cli@latest --pptx docs/presentation-marp.md
```
